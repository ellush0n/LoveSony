     import React from 'react';
     import ReactDOM from 'react-dom';
     import App from './App'; // Убедитесь, что у вас есть файл App.js в той же папке
     import './index.css'; // Если у вас есть стили

     ReactDOM.render(
       <React.StrictMode>
         <App />
       </React.StrictMode>,
       document.getElementById('root')
     );
     